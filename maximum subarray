class Solution {
    public int maxSubArray(int[] nums) {
        int m=nums[0];
        int a=nums[0];
        for(int i=1;i<nums.length;i++){
            a=Math.max(nums[i],a+nums[i]);
            m=Math.max(a,m);
        }
    return m;    
    }
}
