class Solution {
    public int reverseBits(int n) {
        int rev=0;
        for(int i=31;i>=0;i=i-1){
            int bit=n&1;
            rev=rev+(bit<<i);
            n=n>>1;
        }
        return rev;
    }
}
